<%= form_for @user, :url => {:action => "update_email"} do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <p><%= form.email_field :email, placeholder: user.email, class: "form-field" %></p>
  </div>

  <div class="actions">
    <%= form.submit "Change Email", class: "loginButton" %>
  </div>
<% end %>
