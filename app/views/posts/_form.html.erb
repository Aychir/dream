<%= form_for @post, :url => {:action => "create"} do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class = "field">
    <%= form.text_field :title, placeholder: 'Title', class: 'form-field', :maxlength => 5 %> 
  </div>

  <div id = "media-content" class="field">
  	<%= form.file_field :image, class: 'form-field'%>
  </div>

  <div id = "text-content" class = "field">
    <%= form.cktext_area :caption, ckeditor: { language: 'us'}, :maxlength => 20000 %>
  </div>

  <div class = "field">
    <%= form.text_field :tags, multiple: true, placeholder: 'Add hashtags by typing \'#\' followed by a word! (Ex: #fun)' , class: 'form-field', :maxlength => 1000 %>
  </div>

  <div class="actions">
    <%= form.submit "POST", class: "loginButton" %>
  </div>
<% end %>

